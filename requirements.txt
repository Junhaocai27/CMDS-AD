# Install a torch/torchvision pair matching the target CUDA runtime first.
# CPU-only validation can use the CPU wheels; full training is intended for GPU.
torch>=1.13
torchvision>=0.14

numpy==1.23.1
scipy==1.9.1
scikit-learn==1.2.1
pandas==1.5.3
Pillow==10.3.0
matplotlib==3.8.3
tqdm==4.65.0
rich>=13.0
PyYAML==6.0.1
tifffile==2023.4.12
imageio==2.26.0
opencv-python-headless==4.9.0.80
open3d==0.18.0
timm==1.0.3
wandb==0.15.5

# Optional normal-estimation and image-augmentation stages.
diffusers>=0.25.0
transformers>=4.30.0
accelerate>=0.20.0
safetensors>=0.3.0
einops>=0.6.0

# pointnet2_ops is used by mask_vis_mvtec3dad.py and the legacy multimodal feature code.
# Install the compatible implementation separately for your PyTorch/CUDA build.
